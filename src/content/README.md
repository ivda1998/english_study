# 커리큘럼 콘텐츠 작성 규격

주차 하나가 파일 하나입니다. `week-01.json` … 처럼 두 자리 번호를 쓰고,
이 폴더에 넣으면 앱이 자동으로 읽어갑니다(코드 수정 불필요).

검증은 다음 명령으로 합니다.

```bash
npm run validate:content
```

## 하루 분량

| 항목 | 규격 |
|---|---|
| 어휘 | 정확히 10개. 예문에 표제어가 들어가야 합니다(굴절형이면 원형이 보이도록). |
| 문법 | 포인트 1개 + 한국어 설명(20자 이상) + 예문 2개 이상 + 문항 5개 이상 |
| 독해 | 지문 1편(주차별 단어 수 범위) + 어휘주석 2개 이상 + 전체 해석 + 문항 3개 이상 |
| 듣기 | 안내문 + 스크립트 2줄 이상 + 문항 2개 이상 (+ 받아쓰기 문장) |
| 말하기 | 따라 읽기 2~3문장 + 바꿔 말하기 + 질문 5개 (+ 1분 말하기, 수학 말하기) |

## 꼭 지켜야 하는 규칙

- `days`는 5개이고 순서는 `mon, tue, wed, thu, fri` 입니다.
- `id`는 `w01d1` 형식이며 파일의 주차 번호와 맞아야 합니다.
- `speakingTopics` 5개는 확정된 30개 목록(`src/content/topics.ts`)과 문구·순서가 같아야 합니다.
- **1분 말하기(`oneMinute`)는 화·목·금에만** 넣습니다. 월·수는 넣으면 안 됩니다.
- `oneMinute.topic`은 그 주 주제 5개 중 하나여야 하고, `keywords`는 정확히 5개입니다.
- **수학 연결 말하기(`mathTalk`)는 주 1회 이상** 넣습니다. `basic`과 `upgraded`는 각각 3문장입니다.
- 따라 읽기(`shadowing`) 문장은 그날 지문 본문에 그대로 들어 있어야 합니다.
- 객관식 정답은 0부터 세는 인덱스이고, 보기는 서로 달라야 합니다.
- 모든 채점 문항에는 한국어 해설을 답니다.

## 지문 길이 (주차별, 단어 수)

| 주차 | 범위 |
|---|---|
| 1 | 95 ~ 130 |
| 2 | 105 ~ 140 |
| 3 | 115 ~ 150 |
| 4 | 125 ~ 160 |
| 5 | 135 ~ 172 |
| 6 | 150 ~ 195 |

## 문항 종류

```jsonc
// 4지선다
{ "kind": "choice", "id": "w01d1-g1", "prompt": "…", "options": ["…"], "answer": 0, "explanation": "…" }

// 단답 (대소문자·문장부호는 무시하고 채점)
{ "kind": "write", "id": "…", "prompt": "…", "accept": ["has lived"], "explanation": "…", "hint": "…" }

// 채점하지 않는 자기 서술
{ "kind": "free", "id": "…", "prompt": "…", "sample": "…" }
```

## 지문 출처

지문은 기출이나 교재에서 가져오지 않고 직접 씁니다. 소재가 실제 연구나 사건이라면
`passage.source`에 근거를 적으세요. 지문 아래에 작게 표시되고, `url`이 있으면 링크가 걸립니다.

```jsonc
"source": {
  "label": "Sullivan & Birch, 'Pass the sugar, pass the salt', Developmental Psychology 26(4), 1990",
  "url": "https://..."
}
```

**확인하지 않은 수치는 쓰지 마세요.** 그럴듯한 숫자를 지어 넣으면 학습자가 그것을 연구 결과로
받아들입니다. 근거를 못 찾으면 수치를 빼고 일반적인 표현으로 쓰거나, 아예 다른 소재를 고르세요.
정설이 아닌 이야기라면 그 사실 자체를 지문 안에 밝히는 편이 낫습니다 — 오히려 더 좋은 읽을거리가 됩니다.

### 배포 전 사실 검증

주차 콘텐츠를 새로 만들거나 지문을 고쳤다면 **배포 전에** 검증 에이전트를 돌리세요.
`.claude/agents/fact-checker.md` 에 정의되어 있습니다.

```
> fact-checker 에이전트로 week-02.json 지문을 검증해줘
```

에이전트는 지문이 사실로 서술하는 내용을 원 연구와 대조하고, 지어낸 수치·정설이 아닌 서술·
출처와 본문의 불일치를 보고합니다. 파일은 고치지 않으니 판단은 사람이 합니다.
"확인하지 못함"으로 남은 항목은 통과가 아니라 **보류**로 다루세요.

## 밑줄 표기

시험 문항은 "밑줄 친 부분"처럼 특정 구간을 가리킵니다. 그 구간을 `[[ ]]` 로 감싸면
화면에 밑줄로 그려집니다. **지시문에 "밑줄"이라고 썼는데 표기가 없으면 검증에서 막힙니다** —
어디를 가리키는지 알 수 없어 문제를 풀 수 없기 때문입니다.

```jsonc
// 지시문 안의 예문에 긋기
{ "prompt": "밑줄 친 부분을 바르게 고쳐 쓰세요.\nYou look [[tiredly]] today. → You look ______ today." }

// 보기마다 긋기 — "나머지와 다른 것" 유형은 보기 전부에 그어야 합니다
{ "prompt": "밑줄 친 부분이 어법상 틀린 것은?",
  "options": ["She looks [[happy]].", "It smells [[sweet]].", "He feels [[sadly]].", "The plan sounds [[good]]."] }
```

지문(`reading.passage.body`)에도 그을 수 있습니다. 밑줄 표기는 소리와 채점에서는
자동으로 걷어내므로, 지문 듣기·따라 읽기·받아쓰기에는 영향을 주지 않습니다.
단어 수 검증에도 포함되지 않습니다.

## 듣기 대화

`listening.script`의 각 줄에 `speaker`를 넣으면 화자마다 다른 목소리로 읽습니다.
기기에 영어 목소리가 하나뿐이면 음 높이를 달리해 구분합니다. **두 사람 대화는
반드시 `speaker`를 넣으세요** — 없으면 한 사람이 혼자 말하는 것처럼 들립니다.

```jsonc
"script": [
  { "speaker": "Mina", "text": "What is your favorite subject?" },
  { "speaker": "Jiho", "text": "Math. It feels like a puzzle." }
]
```

앱의 **설정 → 주차 콘텐츠 가져오기**에서 이 형식의 JSON을 붙여넣으면
빌드 없이 바로 같은 번호의 주차를 대신하게 할 수 있습니다.
